// frontend/src/simple.test.jsx
import "@testing-library/jest-dom";

describe("Simple Frontend Test", () => {
  it("should also always pass", () => {
    // This test checks if a simple math operation is correct.
    expect(1 + 1).toBe(2);
  });
});
